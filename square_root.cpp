#include<iostream>
#include<cmath>

using namespace std;

int main()
{
        double number;
        cout<<"Enter a number to find the square root"<<endl;
        cin>>number;
        cout<<"Square rooot of "<<number<<"= "<<sqrt(number)<<endl;

        return 0;
}